interface:
  display_name: "Audit Smogon Calc Upstream"
  short_description: "Audit upstream calc and CC compatibility patches"
  default_prompt: "Use $audit-smogon-calc-upstream to compare the pinned calculator and local compatibility paths with frozen upstream HEAD, then recommend native adoption, patch retention, rebase, retirement, or deferral."
